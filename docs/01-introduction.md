# Amazon CloudFront Overview

Amazon CloudFront is a global Content Delivery Network (CDN) that delivers web content such as HTML, CSS, JavaScript, and images with low latency and high transfer speed.

It works by caching content at edge locations worldwide, bringing data closer to users.

## What is CDN?

A CDN is a network of distributed servers (edge locations) that deliver content faster based on user location.
