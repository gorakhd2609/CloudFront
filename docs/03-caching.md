# Caching in CloudFront

Caching means storing frequently accessed data in edge locations.

## Example:
- First request → fetch from origin
- Next request → served from cache (faster)

## Benefits:
- Faster response time
- Reduced load on origin server
- Better performance
