# Use Cases

## 1. CloudFront + S3
Used for:
- Static websites
- Images & videos

Flow:
User → CloudFront → S3

## 2. CloudFront + ELB + ASG
Used for:
- Dynamic applications

Flow:
User → CloudFront → Load Balancer → Auto Scaling Group
